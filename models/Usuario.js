export default class Usuario {
    constructor(cod, acesso, cpf, nomeCompleto) {
        this.cod = cod;
        this.cpf = cpf;
        this.acesso = acesso;
        this.nomeCompleto = nomeCompleto
    }
}